{{- $corsOrigins := .Values.taskdesk.env.corsOrigins -}}
{{- $corsOriginsValue := "" -}}
{{- if kindIs "slice" $corsOrigins -}}
  {{- $corsOriginsValue = join "," $corsOrigins -}}
{{- else if $corsOrigins -}}
  {{- $corsOriginsValue = $corsOrigins -}}
{{- else if .Values.taskdesk.env.clientUrl -}}
  {{- $corsOriginsValue = .Values.taskdesk.env.clientUrl -}}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "taskdesk.fullname" . }}
  labels:
    {{- include "taskdesk.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "taskdesk.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "taskdesk.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: taskdesk
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "taskdesk.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      enableServiceLinks: {{ default false .Values.enableServiceLinks }}
      containers:
        - name: taskdesk
          securityContext:
            {{- toYaml .Values.taskdesk.securityContext | nindent 12 }}
          image: "{{ .Values.taskdesk.image.repository }}:{{ .Values.taskdesk.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.taskdesk.image.pullPolicy }}
          ports:
            - name: taskdesk
              containerPort: {{ .Values.taskdesk.service.targetPort }}
              protocol: TCP
          env:
            - name: KANEO_CLIENT_URL
              value: "{{ .Values.taskdesk.env.clientUrl }}"
            - name: CORS_ORIGINS
              value: {{ $corsOriginsValue | quote }}
            - name: AUTH_SECRET
              {{- if .Values.taskdesk.env.existingSecret.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.taskdesk.env.existingSecret.name }}
                  key: {{ .Values.taskdesk.env.existingSecret.key }}
              {{- else }}
              value: {{ .Values.taskdesk.env.authSecret | quote }}
              {{- end }}
            {{- if and (not .Values.taskdesk.env.database.external.enabled) .Values.postgresql.auth.existingSecret }}
            - name: KANEO_POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postgresql.auth.existingSecret }}
                  key: {{ .Values.postgresql.auth.secretKeys.userPasswordKey }}
            {{- end }}
            - name: DATABASE_URL
              {{- if .Values.taskdesk.env.database.external.enabled }}
              {{- if .Values.taskdesk.env.database.external.existingSecret.enabled }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.taskdesk.env.database.external.existingSecret.name }}
                  key: {{ .Values.taskdesk.env.database.external.existingSecret.passwordKey }}
              {{- else }}
              value: "postgresql://{{ include "taskdesk.urlencodeUserinfo" .Values.taskdesk.env.database.external.username }}:{{ include "taskdesk.urlencodeUserinfo" .Values.taskdesk.env.database.external.password }}@{{ .Values.taskdesk.env.database.external.host }}:{{ .Values.taskdesk.env.database.external.port }}/{{ .Values.taskdesk.env.database.external.database }}"
              {{- end }}
              {{- else }}
              {{- if .Values.postgresql.auth.existingSecret }}
              value: "postgresql://{{ include "taskdesk.urlencodeUserinfo" .Values.postgresql.auth.username }}:$(KANEO_POSTGRES_PASSWORD)@{{ include "taskdesk.fullname" . }}-postgresql:{{ .Values.postgresql.service.port }}/{{ .Values.postgresql.auth.database }}"
              {{- else }}
              value: "postgresql://{{ include "taskdesk.urlencodeUserinfo" .Values.postgresql.auth.username }}:{{ include "taskdesk.urlencodeUserinfo" .Values.postgresql.auth.password }}@{{ include "taskdesk.fullname" . }}-postgresql:{{ .Values.postgresql.service.port }}/{{ .Values.postgresql.auth.database }}"
              {{- end }}
              {{- end }}
            - name: DISABLE_REGISTRATION
              value: {{ .Values.taskdesk.env.disableRegistration | quote }}
            - name: DISABLE_PASSWORD_REGISTRATION
              value: {{ .Values.taskdesk.env.disablePasswordRegistration | quote }}
            - name: DISABLE_EMAIL_OTP_SIGN_IN
              value: {{ .Values.taskdesk.env.disableEmailOtpSignIn | quote }}
            {{- with .Values.taskdesk.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.taskdesk.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.taskdesk.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.taskdesk.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.taskdesk.resources | nindent 12 }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
