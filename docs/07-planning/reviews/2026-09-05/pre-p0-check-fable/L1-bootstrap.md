> **Audit trail only — not a stage-2 gate section.** Every row below was applied in its owning document or recorded in the decision log on 2026-09-06 (see decision-log.md "Pre-P0 check applied"). Line numbers refer to the documents as they stood on 2026-09-05, before those edits.

# L1 Bootstrap findings — TaskDesk v2 docs vs real kaneo (HEAD 51255e85)

| severity | doc file:line | doc says | actually true (evidence) | fix |
|---|---|---|---|---|
| high | docs/04-engineering/repository-bootstrap.md:19-27 | "What is copied, what is not" table — 7 Copy rows / 7 Do-not-copy rows | The table covers ~14 of kaneo's ~40 root entries. Real root (`ls -A ../kaneo`) also contains: `.agents .claude .cursor .coderabbit.yaml .devcontainer .husky .npmrc .vscode .dockerignore .gitattributes AGENTS.md CLAUDE.md ENVIRONMENT_SETUP.md CODE_OF_CONDUCT.md CONTRIBUTING.md README.md SECURITY.md .env.sample compose.yml compose.local.yml scripts/ tests/ sentry/ skills-lock.json pnpm-lock.yaml package.json` — none of which appear in either column | Make the table exhaustive: enumerate `ls -A` of the kaneo snapshot and give every entry a verdict; add an explicit "if it is not in this table, do not copy it" rule so an agent cannot default to `cp -r` |
| high | docs/04-engineering/repository-bootstrap.md:19-27 | table is silent on `AGENTS.md`, `CLAUDE.md`, `.claude/`, `.cursor/`, `.agents/` | kaneo ships its own agent instructions at root (`AGENTS.md`, `CLAUDE.md`) plus `.claude/`, `.cursor/`, `.agents/` directories. phases.md:50 lists `AGENTS.md` as a P0 *deliverable we write*, so a copied kaneo `AGENTS.md` would silently become the canonical guide and instruct every later agent with kaneo's conventions (kaneo package names, kaneo pnpm scripts, kaneo branch rules) | Add a Do-not-copy row: `AGENTS.md`, `CLAUDE.md`, `.claude/`, `.cursor/`, `.agents/`, `.coderabbit.yaml`, `.vscode/`, `.devcontainer/` — TaskDesk writes its own; optionally read kaneo's once as reference, never commit it |
| high | docs/04-engineering/repository-bootstrap.md:19-27 + docs/01-architecture/monorepo-layout.md:52-57 | bootstrap table never mentions kaneo's `tests/`; monorepo-layout presents `tests/` as TaskDesk's own five-directory tree | kaneo already has a root `tests/` with `tests/api` and `tests/api-integration`, and its contents test exactly the code the fork deletes: `tests/api/billing/{config,entitlement,subscription-state,trial-identity}.test.ts`, `tests/api/github-integration/`, `tests/api/gitea-integration/`, `tests/api/plugins/{generic-webhook,github,gitea,gitea-ssrf.test.ts}`. Copying `tests/` then deleting billing + the six integration routers leaves red typecheck/unit CI on day one | Give `tests/` an explicit row: copy only `tests/api/{column,database,events,label,redis,scheduler,search,storage,time-entry,user,utils,ws}` and the mcp tests; delete the billing/integration/webhook test directories in the same commit as the router deletions, and list them in the removal checklist |
| medium | docs/04-engineering/repository-bootstrap.md:24 | copies `biome.json, turbo.json, pnpm-workspace.yaml, commitlint.config.js, release.config.js` and 26 says GitHub workflows are rewritten | All five files exist at kaneo root, but the workflows that consume them do not travel: `.github/workflows/` has 15 workflows (`release.yml`, `publish-mcp.yml`, `publish-planka-import.yml`, `update-contributors.yml`, `deploy-site.yml`, `helm-chart.yml`, `auto-assign.yml`, `auto-merge.yml`, …). `release.config.js` + `commitlint.config.js` bring kaneo's semantic-release setup (root `package.json` devDeps `semantic-release`, `@semantic-release/*`, `husky`, `"prepare": "husky"`) and `.husky/` — which the table also omits | Decide explicitly whether TaskDesk keeps semantic-release + commitlint + husky; if yes, copy `.husky/` and say so; if no, drop `release.config.js`/`commitlint.config.js` and the `prepare` script rather than copying dead config |
| medium | docs/04-engineering/repository-bootstrap.md:19 | Do-not-copy lists `compose.coolify.yml` only | kaneo root has three compose files: `compose.yml`, `compose.local.yml`, `compose.coolify.yml`. monorepo-layout.md:70 wants `compose.yml` = "local development stack" but bootstrap §6 (line 92-93) runs `cp deploy/.env.example .env` and `scripts/deploy.sh local`, neither of which exists in kaneo (`ls deploy` → only `kaneo-entrypoint.sh`; `ls scripts` → `i18n/ release/ provision-sentry-alerts.sh provision-sentry-dashboards.sh`) | State that all three compose files, `deploy/.env.example` and `scripts/deploy.sh` are **written fresh**, not inherited; do-not-copy `compose.yml`, `compose.local.yml`, `compose.coolify.yml` |
| medium | docs/04-engineering/repository-bootstrap.md:19 | `scripts/` is not in either column; monorepo-layout.md:62-63 lists a `scripts/` full of TaskDesk-only files | kaneo's `scripts/` contains `i18n/` (the check/report/schema scripts the root `i18n:check` scripts and CI's i18n gate call), `release/`, and `provision-sentry-alerts.sh` / `provision-sentry-dashboards.sh` (Sentry — already a do-not-copy elsewhere) | Split the row: copy `scripts/i18n/` (needed by `pnpm i18n:check`, which CI runs), do not copy `scripts/release/` or the two `provision-sentry-*.sh` |
| medium | docs/01-architecture/inherited-features.md:30 | "`skills/` — `improve-animations`, `find-animation-opportunities` … the other five skills there are ours to write" | kaneo `skills/` holds **10** skill directories: `animate animation-vocabulary apple-design coss emil-design-eng find-animation-opportunities improve-animations pick-ui-library prototype review-animations`, plus a root `skills-lock.json` manifest | Correct the row to 10, name the 8 not kept, and give `skills-lock.json` a verdict (it pins kaneo's skill set and will be stale the moment 8 skills are dropped) |
| high | docs/04-engineering/repository-bootstrap.md:36-41 (§2 De-brand) | de-brand covers "Package names", "Strings", "Assets", "References: GHCR image names, Docker Hub links, the Sentry DSN, Coolify labels" | **Environment variables are not mentioned at all.** `grep -rhoE 'process\.env\.[A-Z0-9_]+' apps/api/src \| sort -u` returns **72** distinct variables, none prefixed `TASKDESK_`: 4 `KANEO_*` (`KANEO_CLIENT_URL`, `KANEO_API_URL`, `KANEO_INTERNAL_API_URL`, `KANEO_CLOUD`, plus `KANEO_ALLOW_PRIVATE_WEBHOOK_DESTINATIONS`), and bare `AUTH_SECRET`, `DATABASE_URL`, `POSTGRES_{DB,USER,PASSWORD,HOST,PORT}`, `REDIS_*` (7), `S3_*` (3), `SMTP_*`, `COOKIE_DOMAIN`, `TRUSTED_PROXIES`, `NOTIFICATION_SECRET_ENCRYPTION_KEY`, `DEVICE_AUTH_CLIENT_IDS`, `CUSTOM_OAUTH_*` (9), `DISABLE_*` (6), `TURNSTILE_*`, `SENTRY_*` (5), `CREEM_*`/`BILLING_*` (6). monorepo-layout.md:162 mandates `TASKDESK_*` and configuration-reference.md:23-38 names 5 required + ~8 optional `TASKDESK_*` vars | Add an explicit env-rename table to §2 (kaneo name → TaskDesk name → dropped), and note that renaming touches `.env.sample`, `Dockerfile.kaneo`, `deploy/kaneo-entrypoint.sh`, `charts/kaneo/values.yaml`, all three compose files, `apps/api/src/auth.ts`, `apps/api/src/database/*`, and `apps/web`'s `VITE_*`/`env.sh` substitution |
| high | docs/04-engineering/repository-bootstrap.md:92 | `cp deploy/.env.example .env  # five required variables` | kaneo has no `deploy/.env.example` (only `deploy/kaneo-entrypoint.sh`); the real file is root `.env.sample`, which is ~97 lines and defines a *different* variable set. The gap between 72 real vars and 5 required TASKDESK_ vars is the actual size of the de-brand task, and no doc enumerates it | Say `.env.sample` is rewritten from scratch as `deploy/.env.example`; list which of the 72 become TASKDESK_*, which move into the database as plugin config, and which are deleted with their feature |
| high | (absent) docs/01-architecture/inherited-features.md:13-31 has no row; docs/04-engineering/repository-bootstrap.md §2 has no line | Guest / anonymous access is never named in any of the eight reviewed docs (`grep -rni "guest\|DISABLE_GUEST\|DEMO_MODE" docs` → only Plane-import role rows) | kaneo enables better-auth's `anonymous()` plugin **by default**: `apps/api/src/auth.ts:275-283` — `...(process.env.DISABLE_GUEST_ACCESS !== "true" ? [anonymous({ generateName: …, emailDomainName: "kaneo.app" })] : [])`. It is opt-**out**, so a copied kaneo ships anonymous account creation unless the var is set. Also `apps/api/src/utils/get-settings.ts:15,27` exposes `isDemoMode` (`DEMO_MODE`) and `hasGuestAccess` to the client, and `apps/api/src/auth.ts:578-586` deliberately exempts anonymous users from registration limits | Add an inherited-features row: `anonymous()` guest sign-in + `DEMO_MODE` → **Remove at fork**, same reasoning as `public-project` (an unauthenticated/ephemeral-identity surface must not ship dormant). Note the `emailDomainName: "kaneo.app"` string is also a de-brand hit |
| low | docs/08-docs-site/plan.md:54 | `configuration  the eight bootstrap variables` | repository-bootstrap.md:92 says "five required variables" and configuration-reference.md:23-27 lists exactly five required (`TASKDESK_DATABASE_URL`, `_ENCRYPTION_KEY`, `_AUTH_SECRET`, `_AGENT_URL`, `_PORTAL_URL`) | Make it "five required variables" (or say five required + three commonly set) so the docs site and the bootstrap page agree |
| high | docs/01-architecture/inherited-features.md:23 | "`scheduler` (`croner`), `mcp`, `oauth`, … `storage`, **`plugins`**, `instance` … Keep | The mechanisms v2 assumes; **`job_lease` is ours to write**" | Two errors in one row. (a) `apps/api/src/plugins/` is **not** a generic plugin registry — `ls apps/api/src/plugins` → `discord/ generic-webhook/ gitea/ github/ slack/ telegram/ index.ts registry.ts types.ts`, and `apps/api/src/plugins/index.ts:1-22` registers exactly the six integrations the same doc deletes at lines 15-17. (b) `job_lease` already exists: `apps/api/src/database/schema.ts:480` `export const jobLeaseTable = pgTable("job_lease", { name, owner, expiresAt })`, added post-v2.22.0 by "replace the seat-reconciliation advisory lock with a job lease" | Split the row: keep `plugins/registry.ts` + `types.ts` as the seed of `packages/plugins-contracts`, delete the six plugin subdirectories with their routers; change "`job_lease` is ours to write" to "`job_lease` arrives with the snapshot at `schema.ts:480` — review and adopt, do not re-invent" |
| high | docs/01-architecture/inherited-features.md:20 + docs/04-engineering/repository-bootstrap.md:52-56 | `public-project` … "Router and screens deleted in P0 step 1" | `public-project` is not a router directory, so "delete the router" under-scopes it. The real surface is: the inline route `api.get("/public-project/:id", …)` at `apps/api/src/index.ts:226`; `apps/api/src/project/response.ts`; the **`project.is_public` column** at `apps/api/src/database/schema.ts:328` (`isPublic: boolean("is_public").default(false)`) and the migration that added it; `apps/web/src/components/public-project/`, `apps/web/src/routes/public-project.$projectId.tsx` and its fetchers/hooks. There is a second unauthenticated route the docs never mention: `api.get("/invitation/public/:id")` at `apps/api/src/index.ts:240` | Rewrite the verdict as a checklist naming `index.ts:226`, the web route/components, and a two-phase drop of `project.is_public` per migrations.md; add a separate verdict for the unauthenticated `/invitation/public/:id` route (keep with a policy, or remove) |
| medium | docs/04-engineering/repository-bootstrap.md:43 + docs/01-architecture/inherited-features.md:24 | "strip billing … `workspace_billing` / `trial_grant` tables and their migrations" | The billing footprint is four tables, not two: `workspaceBillingTable` (schema.ts:178), `trialGrantTable` ("trial_grant", :211), `billingEventTable` ("billing_event", :217), `billingReminderSentTable` ("billing_reminder_sent", :444) — plus env `BILLING_TRIAL_DAYS`, `BILLING_FOUNDING_CUTOFF`, `BILLING_REMINDER_MAX_PER_RUN`, `CREEM_*`, and `apps/api/src/utils/get-settings.ts:3,31` importing `isBillingEnabled` into the public settings payload | Name all four tables, the four billing env vars, and `get-settings.ts`'s `billingEnabled` field in the strip list |
| medium | docs/01-architecture/inherited-features.md:15-17 | six integrations "Remove at fork … Routers, handlers, screens and dependencies deleted" | The database side is never mentioned: `githubIntegrationTable` ("github_integration", schema.ts:845) and the generic `integrationTable` ("integration", schema.ts:867, `type` + encrypted `config`, unique on `(project_id, type)`) which is what slack/discord/telegram/gitea/generic-webhook rows live in (`apps/api/src/plugins/slack/index.ts:13` `type: "slack"`). Also `NOTIFICATION_SECRET_ENCRYPTION_KEY` and `KANEO_ALLOW_PRIVATE_WEBHOOK_DESTINATIONS` exist only for them | Add "drop `github_integration` and `integration` tables (or keep `integration` as the plugin-config table if `plugins/registry.ts` is retained — decide explicitly), and the associated env vars" |
| medium | docs/04-engineering/migrations.md:10-22 | `apps/api/drizzle/ 0001_initial.sql … custom/ … meta/ … snapshots/` — a fresh numbering starting at 0001 | The doc never states whether kaneo's migration history is inherited or squashed. Reality: `ls apps/api/drizzle` → **45 `.sql` files** `0000_confused_pixie.sql` … `0044_*`, plus `meta/` with 39 snapshot JSONs and `_journal.json`. There is no `custom/` and no `snapshots/` directory. Starting at `0001_initial.sql` implicitly means squashing to a fresh baseline and regenerating `meta/_journal.json` — a real decision with no ADR | Add one sentence to migrations.md: "kaneo's 45 migrations are **not** inherited; at the fork the schema is squashed to `0001_initial.sql` generated from the post-strip `schema.ts`, and `drizzle/meta/` is regenerated" (or the opposite) — and say so in repository-bootstrap.md §2, since the strip removes tables those 45 migrations create |
| medium | docs/04-engineering/migrations.md:26-28,50 | "Hand-written SQL lives only under `custom/` and is applied by the same runner in interleaved order (the runner sorts by the numeric prefix across both directories)" | `drizzle-kit migrate` (kaneo's `apps/api/package.json` → `"db:migrate": "drizzle-kit migrate"`) applies only what is listed in `drizzle/meta/_journal.json`; it does not scan a second directory. Wrapping it in an entrypoint (as line 50 proposes) does not give it that ability — the interleaved runner has to be written | Say explicitly that the interleaving runner is **new code TaskDesk writes** (not a drizzle-kit feature) and give it a P0 line item, or drop `custom/` in favour of drizzle's supported pattern (hand-edited SQL appended into a generated migration file, journal-tracked) |
| medium | docs/04-engineering/repository-bootstrap.md:70 + docs/01-architecture/monorepo-layout.md:83 | "`packages/permissions` (extend kaneo's)"; layout rule `packages/permissions ──► (nothing — pure)` | kaneo's `packages/permissions` is **84 lines** (`packages/permissions/src/index.ts`): a better-auth `createAccessControl` statement map (`project/task/label/workspace` verbs) and four static roles `viewer/member/admin/owner`, plus `DEFAULT_ROLE_NAMES`. It has **no** capabilities registry, no policy kinds, no evaluator and no route table — and `packages/permissions/package.json` declares `"dependencies": { "better-auth": "^1.6.25" }`, which breaks the "pure, imports nothing" rule the layout asserts | Change "extend kaneo's" to "replace kaneo's": keep the role names for data continuity, but write the capability/policy/evaluator layer fresh and drop the `better-auth` dependency (or record the dependency as an accepted exception in monorepo-layout.md) |
| high | docs/08-docs-site/plan.md:5 | "**Stack:** Next.js + Fumadocs, following kaneo's `apps/site`" | Both halves are wrong at HEAD. (a) `grep -rl fumadocs --include=package.json apps packages` → **no matches**; kaneo uses Fumadocs nowhere. (b) `apps/site` is the **marketing** site: `apps/site/package.json` is `@kaneo/site`, `next: 16.3.0`, `"dev": "next dev -p 3001"`, deps `marked`, `zustand`, `radix-ui`, `@base-ui/react`, `shadcn`, plus `scripts/generate-og.mjs` and a `content/` + `constants/` marketing tree. kaneo's **documentation** is `apps/docs`, a **Mintlify** project (`apps/docs/docs.json` → `"$schema": "https://mintlify.com/docs.json"`, `"theme": "almond"`, `"name": "Kaneo"`, with `api-reference/`, `core/`, `openapi.json`) — and repository-bootstrap.md:21 puts `apps/docs` in the **Do not copy** column | Decide and record: copying `apps/site` gets a Next 16 marketing site, not a docs framework. Either (i) keep `apps/site` as the marketing site and add Fumadocs to it as a new `/docs` segment (say so — "following kaneo" is then untrue), (ii) create `apps/site` fresh as a Fumadocs app, or (iii) reconsider `apps/docs` (Mintlify) — but note plan.md:106-110 wants a Scalar OpenAPI reference and Playwright screenshots, which is a Fumadocs/self-hosted decision, not a Mintlify one |
| high | docs/04-engineering/repository-bootstrap.md:24 (Do not copy `sentry/`) | Sentry removal is scoped to the `sentry/` directory ("kaneo's DSN and project") | `sentry/` holds only `README.md`, `alerts.json`, `dashboards.json`. The code-level footprint is 17 files: `apps/api/src/instrument.ts`, `apps/api/src/index.ts`, `apps/api/src/scheduler/index.ts`, `apps/api/src/utils/authenticate-api-request.ts`, 6 files under `apps/api/src/plugins/*`, `apps/web/src/instrument.ts`, `apps/web/vite.config.ts` (source-map upload), `apps/web/src/query-client/index.ts`, `apps/web/src/components/providers/auth-provider/index.tsx`, **`apps/web/src/components/ui/error-boundary.tsx`**, plus `@sentry/*` deps in both `package.json`s and 5 `SENTRY_*` + 3 `VITE_SENTRY*`/`SENTRY_{AUTH_TOKEN,ORG,PROJECT}` env vars | Broaden the row to "Sentry — remove `sentry/`, both `instrument.ts`, the vite source-map plugin, all `@sentry/*` deps and `SENTRY_*` env". Note the collision with ui-extraction-plan.md:28-29: `components/ui/error-boundary.tsx` is one of the 63 files moving into `packages/ui`, so it must be de-Sentry'd before the move or `packages/ui` inherits an observability dependency (breaking monorepo-layout.md:85) |
| medium | docs/04-engineering/repository-bootstrap.md:25 + docs/01-architecture/monorepo-layout.md:71 | "`Dockerfile.kaneo` → renamed `Dockerfile`"; layout says "`Dockerfile` single multi-stage image" | kaneo has **three** Dockerfiles: `./Dockerfile.kaneo`, `./apps/api/Dockerfile`, `./apps/web/Dockerfile`. Only the root one is given a verdict, so the two per-app Dockerfiles would be copied and then contradict the single-image design (and `apps/api/.dockerignore` + root `.dockerignore` likewise) | Add a Do-not-copy row for `apps/api/Dockerfile` and `apps/web/Dockerfile`; keep/rewrite the two `.dockerignore` files deliberately |
| medium | docs/01-architecture/inherited-features.md:26 + docs/04-engineering/repository-bootstrap.md:60 | "`valibot` alongside Zod; `nanostores` alongside Zustand — **Remove — consolidate**" | Both are mis-stated. `valibot` is a dependency of **`apps/api` only** (`apps/api/package.json:60`), and 7 of its 8 import sites are inside code already slated for deletion (`plugins/{discord,generic-webhook,gitea,github,telegram,slack}/config.ts`, `telegram-integration/controllers/telegram-controller.ts`); the only survivor is `apps/api/src/ws/redis-broadcast-adapter.ts:2`. `nanostores` cannot be "consolidated": `grep -rn 'from "nanostores"' apps packages` → **zero** direct imports; it is better-auth's client store, as `apps/web/src/components/providers/auth-provider/index.tsx:38` states ("Better Auth's useSession uses nanostores rather than TanStack Query") | Rewrite the row: valibot → one file to port after the integration deletions; nanostores → **not removable** while better-auth is the auth library, drop it from the consolidation list (or state the intent as "no app code imports nanostores directly", which is already true) |
| medium | docs/01-architecture/adr/0001-kaneo-as-foundation.md:17-18 | "60+ **Radix-based** primitives" (repeated at docs/01-architecture/monorepo-layout.md:41 "(60+)") | `apps/web/src/components/ui` has 63 files, of which **43 import `@base-ui/react` and exactly 1 (`form.tsx`) imports `@radix-ui`** — while `apps/web/package.json` still lists 18 `@radix-ui/*` dependencies (lines 25-42) that are largely unused by the primitive set. The primitives are Base-UI-based, not Radix-based. `apps/site/package.json` additionally pulls the umbrella `radix-ui@^1.6.7` | Correct the ADR's context sentence to "63 primitives, predominantly Base UI"; ui-extraction-plan.md:16-24 already reaches the right decision but should record that the migration is ~1 file plus **18 stale `@radix-ui/*` package.json entries to prune**, not a 60-component port |
| low | docs/04-engineering/repository-bootstrap.md:99 | exit criterion "`pnpm test:all` is green" | No `test:all` script exists in kaneo (`grep -rn test:all` across all `package.json` + `turbo.json` → none). kaneo's real tasks are `build, dev, lint, typecheck, test, test:integration` (`turbo.json` tasks block) driven by root scripts `build, dev, lint, test, test:integration, typecheck, i18n:check*, i18n:report*, i18n:schema` | Either add `test:all` as an explicit P0 tooling item (with what it aggregates) or state the exit criterion in terms of the CI job list |
| low | docs/01-architecture/monorepo-layout.md:49 (`packages/i18n/`) vs docs/04-engineering/repository-bootstrap.md:23 (copy `i18n/`) | layout puts locale resources in `packages/i18n/`; bootstrap copies kaneo's root `i18n/` | kaneo's locales live at **root** `i18n/`: 18 locale JSONs + `resources.ts` + `schema.json`, consumed by root scripts `i18n:check|report|schema` → `scripts/i18n/*.mjs`, and gated by CI (`.github/workflows/ci.yml` job `i18n`, step "Check locale files against en-US"). Moving them to `packages/i18n/` means rewriting those three scripts and the CI step | Say which location wins, and if it is `packages/i18n/`, list "port `scripts/i18n/*.mjs` and the CI i18n job" as a bootstrap task |
| medium | docs/04-engineering/repository-bootstrap.md:62-66, docs/07-planning/phases.md:39-44 | "retrofit **every** inherited kaneo router into the five policy kinds … the route-coverage test … goes green on the *inherited* surface" — no size given | The inherited surface is measurable: `apps/api/src/index.ts` mounts **28** sub-routers via `.route(` plus 8 inline `api.get/post(...)` routes (36 registrations total), and `grep -rnE '\.(get\|post\|put\|patch\|delete)\(\s*"' apps/api/src` returns **105** route definitions. Roughly 20 of those belong to code being deleted (billing, the six integrations, public-project), leaving ~85 routes needing a `policy.ts` entry each — and monorepo-layout.md:99-116 requires a `repository.ts` per feature folder too, across the ~30 directories in `apps/api/src` | Put the counts in the doc (28 routers / ~105 routes / ~85 after removals) so the largest P0 task is sized rather than asserted, and state the order: delete first, then retrofit, so no policy is written for a route that is about to go |
| medium | docs/04-engineering/repository-bootstrap.md:19-27 (no row) | nothing said about kaneo's own agent instructions being stale | If `AGENTS.md` is copied it brings kaneo-specific and already-inconsistent guidance: `AGENTS.md:69` says "Use pnpm 10.32.1 and **Node.js 20.19 or newer**" while root `package.json` `engines` says `"node": ">=24.0.0"`; `AGENTS.md:63` gives `pnpm --filter @kaneo/api db:generate`; `AGENTS.md:23` describes `apps/site` as "public Next.js site and **documentation host**" (likely the origin of the Fumadocs claim). `CLAUDE.md` is a 5-line pointer that `@AGENTS.md`-includes it | Explicit Do-not-copy for `AGENTS.md`/`CLAUDE.md`, and a note that TaskDesk's AGENTS.md (phases.md:50) is authored fresh — the kaneo one may be read once for its architecture section only |
| low | docs/04-engineering/repository-bootstrap.md:15 | "Confirm kaneo's licence file is MIT and copy it into `THIRD-PARTY-NOTICES.md` verbatim" | True — `LICENSE` line 1 `MIT License`, line 3 `Copyright (c) 2024 Andrej Acevski`. No fix needed; recorded so the notice carries the right holder name | Use "Copyright (c) 2024 Andrej Acevski" verbatim in `THIRD-PARTY-NOTICES.md` and `NOTICE` |
| low | docs/04-engineering/repository-bootstrap.md:22 | copies `packages/email, libs, mcp, permissions, typescript-config`; does not copy `packages/planka-import` | Accurate — `ls packages` → exactly `email libs mcp permissions planka-import typescript-config`. Note `packages/mcp` is a **published** package (`.github/workflows/publish-mcp.yml`, and `publish-planka-import.yml` for the other), so copying it also copies publish plumbing and an npm name | Add "and drop the `publish-mcp` / `publish-planka-import` workflows and any `publishConfig`/npm name in `packages/mcp`" |

## Top 5

1. **The Copy/Do-not-copy table is not exhaustive** (repository-bootstrap.md:19-27) — it gives a verdict to ~14 of ~40 kaneo root entries, silently leaving `AGENTS.md`, `CLAUDE.md`, `.claude/`, `.cursor/`, `.agents/`, `.coderabbit.yaml`, `.husky/`, `.npmrc`, `.devcontainer/`, `.vscode/`, `compose.yml`, `compose.local.yml`, `.env.sample`, `scripts/`, `tests/`, `skills-lock.json` undecided. An agent doing P0 step 1 will copy them by default, and a copied `AGENTS.md` becomes the canonical guide for every later agent.
2. **kaneo's `tests/` tests the code the fork deletes** — `tests/api/billing/*` (4 files), `tests/api/github-integration/`, `tests/api/gitea-integration/`, `tests/api/plugins/{github,gitea,generic-webhook,gitea-ssrf.test.ts}` — so the removal commit turns unit + typecheck CI red unless the test deletions are in the same commit. No doc mentions `tests/` at all.
3. **The de-brand section never mentions environment variables**, yet `apps/api/src` reads **72** of them (`KANEO_*`, bare `AUTH_SECRET`/`DATABASE_URL`/`POSTGRES_*`/`REDIS_*`/`S3_*`/`SMTP_*`, `CUSTOM_OAUTH_*` ×9, `DISABLE_*` ×6, `SENTRY_*` ×5, `CREEM_*`/`BILLING_*` ×6) against a promised five required `TASKDESK_*`. No doc contains the rename/drop mapping, and `deploy/.env.example` (bootstrap.md:92) does not exist in kaneo — the file is root `.env.sample`.
4. **Guest/anonymous access is nowhere in the docs.** `apps/api/src/auth.ts:275-283` enables better-auth's `anonymous()` plugin **unless** `DISABLE_GUEST_ACCESS=true`, i.e. opt-out; `get-settings.ts:15,27` also ships `DEMO_MODE`/`hasGuestAccess` to the client. A fork that deletes `public-project` for being an unauthenticated surface but keeps default-on anonymous account creation has not closed the class of hole it set out to close.
5. **Two "follow kaneo" claims are false at HEAD and change what P0 builds:** the docs site (`plan.md:5` "Next.js + Fumadocs, following kaneo's `apps/site`" — kaneo has zero Fumadocs; `apps/site` is a Next 16 marketing site and the real docs are `apps/docs` on Mintlify, which bootstrap.md:21 tells you not to copy), and `packages/permissions` ("extend kaneo's" — it is 84 lines of better-auth `createAccessControl` with a `better-auth` dependency, no policy kinds, no evaluator, and it violates the layout's "pure, imports nothing" rule). Related: `inherited-features.md:23` keeps "`plugins`" while deleting the six integrations that `apps/api/src/plugins/index.ts:1-22` *is*, and says "`job_lease` is ours to write" when it already exists at `schema.ts:480`.
