> **Audit trail only — not a stage-2 gate section.** Every row below was applied in its owning document or recorded in the decision log on 2026-09-06 (see decision-log.md "Pre-P0 check applied"). Line numbers refer to the documents as they stood on 2026-09-05, before those edits.

# L6 — Cross-document consistency of the planning layer (TaskDesk v2)

Scope: does every planning document agree on WHICH phase each feature lands in, what is deferred, what is inherited, and what the counts are.
Out of scope by standing rule: the calendar/timeline (four-week plan is a flexible target; 3-4 months is fine). Not commented on.

| severity | file A:line says | file B:line says | which should win | concrete fix |
|---|---|---|---|---|
| **high** | `docs/07-planning/phases.md:270-273` lists **"AWS Marketplace listing**: container-product packaging, `license.aws-marketplace` plugin ([ADR 0013]), seller registration and security review" as an unqualified **P7 deliverable**, with no deferral note anywhere in the P7 section | `docs/07-planning/roadmap.md:84` "**AWS Marketplace** \| Not implemented" under the heading `roadmap.md:75` "**Explicitly deferred beyond the current three-to-four-month scope (decided 2026-09-05)**", and `docs/07-planning/accelerated-delivery-plan.md:173-176` "**AWS Marketplace** — **deferred beyond this window** (decided 2026-09-05) … Nothing starts before a P7 listing decision is taken" | The **deferral** wins. Thomas's 2026-09-05 decision defers AWS Marketplace beyond the current 3–4-month scope (BYOL/contract preferred). phases.md is the only file that still schedules it as ordinary P7 work | In `phases.md` P7, replace the bullet with: "**AWS Marketplace listing — deferred beyond the current scope (decided 2026-09-05).** Not built in P7. When a listing decision is taken, BYOL/contract is preferred over usage metering; the `license` plugin kind and [ADR 0013] are the extension point. See roadmap.md (`roadmap.md#explicitly-deferred-beyond-the-current-three-to-four-month-scope`)." |
| **high** | `docs/07-planning/release-plan.md:75` maps `2.3.0` / P7 to "+ full a11y audit, i18n, penetration test, **marketplace packaging**", sellable to "**External paying customers and AWS Marketplace buyers**"; `release-plan.md:108-109` repeats "marketplace packaging" in the list of "**Features deferred to `2.1`–`2.3`**"; and `release-plan.md:89` states "`2.1`–`2.3` then land **inside the 3-month window**" | `docs/07-planning/accelerated-delivery-plan.md:88` "(AWS Marketplace is deferred **beyond this window** — decided 2026-09-05)" — the window being weeks 6–17, Oct 10 – Dec 31 | The **deferral** wins. release-plan.md schedules marketplace packaging into `2.3.0`, and separately asserts `2.3` lands inside the very window the accelerated plan says marketplace is deferred beyond — a direct contradiction inside one release table | In `release-plan.md:75`, cut "marketplace packaging" from the `2.3.0` contents and change "Sellable to" to "**External paying customers** (self-hosted). A marketplace listing is deferred beyond the current scope"; in `release-plan.md:108-109` move "marketplace packaging" out of the "deferred to 2.1–2.3" list into a separate line "deferred beyond the 3-month window entirely" |
| **medium** | `docs/07-planning/roadmap.md:84` deferral row gives "**Revisit when: P7**" — i.e. it schedules the revisit inside the plan, since `accelerated-delivery-plan.md:170` puts "**P7 — Full polish and hardening**" in weeks 6–17 (Oct 10 – Dec 31) | The same table's own heading, `roadmap.md:75`, says these items are "**deferred beyond the current three-to-four-month scope**" — and `accelerated-delivery-plan.md:173` says marketplace is deferred *beyond* the weeks 6–17 window that contains P7 | The **heading** wins; the "Revisit when: P7" cell contradicts the table it sits in | Change the `roadmap.md:84` "Revisit when" cell from "P7" to "**After the current scope closes** — a separate listing decision, not a P7 task" |
| **medium** | `docs/07-planning/status.md:174` Open decisions row: "~~Whether to sell externally~~ \| **Decided: yes, via AWS Marketplace** — see [decision log]" — no deferral qualifier at all | `docs/07-planning/roadmap.md:98-99` carries the *same* decision **plus** a second row that qualifies it: "**AWS Marketplace seller registration** \| Only once a P7 listing decision is taken — the listing is **deferred** beyond the current scope (2026-09-05) … BYOL/contract preferred" | roadmap.md's qualified pair wins. status.md is the "first thing anyone reads cold" (`status.md:8-9`), so the unqualified row is the one most likely to make an agent start marketplace work | Append to `status.md:174`: "— **the listing itself is deferred beyond the current 3–4-month scope (2026-09-05); BYOL/contract preferred when it comes.**" |
| **high** | `docs/07-planning/decision-log.md:388` entry "**AWS Marketplace is the first external sales channel; metering is an optional plugin**" — `:390` "pursue an AWS Marketplace container-product listing as the first externally sellable channel", `:401` "Packaging and seller-registration work is tracked in [AWS Marketplace listing] and **lands in P7**". **No supersession note on the entry** | The same file, `docs/07-planning/decision-log.md:57-58`, later index entry: "**Deferred beyond the current three-to-four-month scope** … **AWS Marketplace (prefer BYOL/contract when it comes)**"; and `roadmap.md:84`, `accelerated-delivery-plan.md:173` | The **later** entry wins (it records Thomas's confirmed 2026-09-05 decision document). The Marketplace entry is stale but unmarked — the log has a house style for this: `decision-log.md:196` "**Superseded the same day by section N…**" and `:234` "**Partly superseded the same day**" | Add to `decision-log.md` under the Marketplace entry (after `:401`): "**Superseded the same day by the confirmed decisions of 2026-09-05 (above):** the *listing* is deferred beyond the current three-to-four-month scope and a **BYOL / contract** model is preferred over usage metering ([ADR 0013] addendum). What stands from this entry is only the `license` **plugin kind** as the architecture." |
| **medium** | `docs/07-planning/decision-log.md:514` entry "**No dates on the roadmap until P1 closes**", `:516` "the roadmap sequences phases but gives no dates", `:518-520` "Dates now would be fiction … After P0 and P1 there is evidence". No supersession note | `docs/07-planning/accelerated-delivery-plan.md:83-88` gives a full dated calendar (weeks 1–17, Sep 5 – Dec 31) **before P1 closes**, and `decision-log.md:27` records section A as an operating rule about that calendar | The entry is **partly** superseded: `roadmap.md:6` genuinely still carries no dates, so the *roadmap* half stands; the "until P1 closes" half does not — a dated plan exists now, in a separate document | Append to `decision-log.md:520`: "**Partly superseded the same day:** the roadmap still carries no dates, but the dated mapping was requested and now lives in accelerated-delivery-plan.md (`accelerated-delivery-plan.md`); the 'until P1 closes' condition no longer applies." (No comment intended on the calendar itself — this is bookkeeping only.) |
| **medium** | `docs/07-planning/accelerated-delivery-plan.md:185-193` correction note: "several rows below **understate what exists at go-live**, because kaneo *already ships* them and they arrive with the fork in week 1: **calendar and gantt/timeline views, basic time entries, and an automation-rule engine (`workflow-rule`)** … 'deferred' means *inherited but feature-flagged off until the v2 spec is aligned*, not 'not present'" | The register rows **immediately below were not corrected**: `accelerated-delivery-plan.md:198` Agile → deferred "Modules, estimates, **calendar/timeline views**"; `:199` Time & cost → at go-live "**Not present**", deferred "Time entries, rates, budgets — **the whole feature**"; and there is **no row at all** for automations / `workflow-rule` | The **correction note** wins — `decision-log.md:244-246` states outright that "the accelerated plan's deferral register overstated what was missing (calendar/gantt/time entries/automations are inherited in week 1, not built in month three); **that register is corrected**." It was annotated, not corrected | Rewrite the three rows: Agile → at go-live "Cycles; **calendar/timeline views inherited from kaneo, flagged off**"; Time & cost → at go-live "**Basic time entries inherited from kaneo, flagged off**", deferred "rates, budgets, timesheet grid, the v2-spec alignment"; add a row "**Automations** \| `workflow-rule` engine **inherited, flagged off** \| The v2 `AM-n` automations spec with dry-run" |
| **medium** | `docs/07-planning/phases.md:224` P5 lists "Calendar and timeline layouts" and `:225` "Time entries, timer, timesheet grid" as plain P5 build items; `:202` P4 lists "Automations with dry-run" — none of the three says the capability **arrives inherited in P0/week 1 and ships flagged off** | `docs/07-planning/accelerated-delivery-plan.md:185-192` and `docs/07-planning/decision-log.md:224-233` ("Any inherited feature without a v2 spec is feature-flagged **off** until its spec exists and it passes the UX gates … `workflow-rule` automations, time entries, gantt and calendar views") | Both are true and neither contradicts the other on *phase*, but phases.md is the "what does done mean" document an implementer reads for P4/P5 and it gives no hint the code is already in the tree from P0 — the exact situation the inherited-features register exists to prevent | In `phases.md`, add one line to each of P4 and P5: "**Inherited from kaneo and flag-gated from P0** (see inherited-features.md (`../01-architecture/inherited-features.md`)): the `workflow-rule` automation engine (P4); calendar/gantt/timeline layouts and basic time entries (P5). These ship **off** until the v2 spec is aligned and the UX gates pass — they are not built from zero here." |
| **medium** | `docs/07-planning/risks.md:344` (R18) header row "**Deadline** \| **Before P7 starts**" and `:349-350` mitigation "**choose a BYOL / contract listing (no usage metering) before P7**" — an open decision with a P7 deadline | `docs/07-planning/decision-log.md:58` "AWS Marketplace (**prefer BYOL/contract when it comes**)" and `roadmap.md:84` "Before P7, **prefer a BYOL / contract** listing" — the choice is already made, and the listing itself is deferred beyond the scope | The **decision** wins: BYOL/contract was chosen on 2026-09-05, so R18's mitigation is complete and its "before P7" deadline is moot | Rewrite R18's deadline cell to "**Decided 2026-09-05** — BYOL/contract preferred; listing deferred beyond the current scope" and restate the mitigation as "**Done:** BYOL/contract chosen over usage metering. Remaining: consult AWS Partner BD on the AGPL position **if and when** a listing decision is taken." |
| **low** | `docs/07-planning/status.md:182-184` watch-list preamble: "Full list in [risks.md], now with **three additions** — **R15–R17** for the accelerated calendar, **R18** marketplace integrity, **R19** pentest lead time, **R20** the kaneo router retrofit" | The same sentence enumerates **six** new risks (R15, R16, R17, R18, R19, R20), all six defined in `docs/07-planning/risks.md:284,299,313,327,344,364` | risks.md wins on the facts; status.md's "three" is a stale word from when only R15–R17 existed | Change "three additions" to "**six additions**" in `status.md:182` |
| **low** | `docs/07-planning/accelerated-delivery-plan.md:230-239` heading "**Risk register additions specific to this plan**" defines **R15, R16, R17** with their own mitigation text, and `:232` says the standing risks are "**R1–R14 and R18–R20**" — i.e. it presents R15–R17 as living here | `docs/07-planning/risks.md:284,299,313` define R15, R16 and R17 in full, with likelihood/impact/owner rows | risks.md is the register (`roadmap.md:102` "Tracked in [risks.md]"; `risks.md:3` "Reviewed at every phase close"). Meanings agree, so this is duplication, not contradiction — but a second home is how registers drift | Replace `accelerated-delivery-plan.md:230-239` with a pointer: "**R15–R17 in risks.md (`risks.md`) are specific to this plan**" plus the one-line summaries, and drop the duplicate mitigation prose. Fix `:232` to say "R1–R14 and R18–R20 are the standing risks; R15–R17 were added for this plan and live in the same register." |
| **high** | `docs/07-planning/accelerated-delivery-plan.md:124-131` — the six workstreams A–F that constitute weeks 2–4 — never mention **custom fields, teams, MCP server, API keys, attachments, search & saved views, relations & hierarchy, projects & engagements, pending actions, impersonation, settings hierarchy, audit trail, or automations**. Verified by grep: `custom field` 0 hits, `Teams` 0, `MCP` 0, `API key` 0, `attachment` 0, `saved view` 0, `relation` 0, `pending action` 0, `Impersonat` 0, `settings hierarchy` 0, `Automation` 0 in the whole file | `docs/07-planning/accelerated-delivery-plan.md:182-183` promises "This is the explicit register … **Nothing in the left column is silently thinner without appearing here**", and `:144` claims the week-4 exit is "**P0–P5** at reduced but real depth" — yet `docs/03-features/README.md:44-52` places 9 of those 13 in **P4** and `phases.md:194-210` lists every one of them as P4 scope | The **register's own promise** wins. 13 of the 31 specs are neither assigned to a workstream nor listed as deferred — the exact silent thinning the document exists to prevent | Either add them to workstream C (governance: teams, custom fields, settings hierarchy, API keys, MCP, impersonation, automations), workstream A (audit trail) and week 1's P1 slice (attachments, relations, saved views, projects, pending actions) — or add explicit deferral rows for each. Do not leave a spec in neither column |
| **medium** | `docs/07-planning/phases.md:118` lists "**Assignment rules**" as **P1** scope | `docs/07-planning/phases.md:138-139` lists, as **P2** scope, "`packages/domain`: SLA engine, service calendars, workflow engine, approvals, **assignment rules** — ported from v1, tested exhaustively" — the same feature, in the same file, in two phases | `docs/03-features/README.md:19` and `docs/03-features/assignment.md:3` both say **P1**, flat, with no P2 carve-out (unlike `projects-and-engagements.md:3` "P1 (delivery structure in P2)", `notifications.md:3` "P4 (in-app inbox in P1)" and `teams.md:3`, which all use the parenthetical convention) | Pick one. If the rules engine really ports in P2, change `assignment.md:3` to "**P1** (the rule *engine* ports with `packages/domain` in P2)" and say so in `README.md:19`; otherwise drop "assignment rules" from `phases.md:139` |
| **medium** | `docs/07-planning/phases.md:231` lists "**Pages**" as a P5 deliverable, and `docs/02-design/screen-inventory.md:50` gives it a route and a phase: "\| Pages \| `/agent/projects/{key}/pages` \| route \| P5 \| ⬜ \|" | `docs/03-features/README.md` has **no row for Pages** in any of its five phase tables (31 features, none named Pages), there is **no `docs/03-features/pages.md`**, and `README.md:113-139`'s rule-id prefix registry has **no prefix** for it | The **feature index** is the register of what gets built — and `README.md:146` states the rule: "**A feature is not built until its spec exists and has been read.**" Pages is scheduled scope with no spec, no rule ids and no owner | Either write `docs/03-features/pages.md` with a prefix (e.g. `PG`), add the row to the P5 table and the registry, and bump the count to 32 — or delete "Pages" from `phases.md:231` and the row from `screen-inventory.md:50`, recomputing the P5 total (28 → 27) and the grand total (136 → 135) |
| **medium** | `docs/07-planning/phases.md:38-39` P0 step 1 reads "Copy kaneo into `Ticketing.v2`; de-brand; strip billing, seats, trials and cloud abuse mitigations; **delete `public-project`** (anonymous boards — removed, not flagged)" — **the integration routers are not named**. `docs/07-planning/status.md:137` repeats the same short form in **Next** step 1 | `docs/07-planning/accelerated-delivery-plan.md:189-192` "kaneo's **Slack/Discord/Telegram/GitHub/Gitea integration routers** and its public project boards are **deleted in P0 step 1**, not flagged off — decided 2026-09-05"; `roadmap.md:85-86` "removed at fork"; `decision-log.md:61-62` "Inherited kaneo integration routers are **removed at fork**, never kept dormant"; `status.md:239` (session log) "delete `public-project` **and the integration routers**" | The **four documents that name the routers** win — it is a confirmed 2026-09-05 decision. phases.md P0 is the authoritative task list an implementer works from, and it omits half the deletion | In `phases.md:39` change to "**delete `public-project` and kaneo's Slack/Discord/Telegram/GitHub/Gitea integration routers** (removed at fork, not flagged off — decided 2026-09-05)"; make the same change to `status.md:137` |
| **medium** | `docs/07-planning/release-plan.md:96-99`: "the accelerated `2.0.0` is defined **by what it deliberately does not yet contain**, and that list lives in **two places that must agree** — [accelerated-delivery-plan.md]'s deferral register, and here" | There is a **third** deferral list: `docs/07-planning/roadmap.md:75-90`, "Explicitly deferred beyond the current three-to-four-month scope (decided 2026-09-05)" — nine rows (antivirus, RLS, marketplace, chat integrations, dev-tool links, public boards, customer self-service IdP config, further IdPs, SCIM `/Bulk`). Only *one* of the nine (marketplace) appears in either of the other two lists | The roadmap table is the one that records Thomas's confirmed decisions (`decision-log.md:57-63` points at it by anchor), so it stands — but release-plan.md's "two places" claim is factually wrong | In `release-plan.md:97-99` change to "**three** places that must agree — roadmap.md (`roadmap.md#explicitly-deferred-beyond-the-current-three-to-four-month-scope-decided-2026-09-05`) for scope deferred beyond the whole program, [accelerated-delivery-plan.md]'s register for what is thinner at go-live, and here." Add the same cross-reference at `accelerated-delivery-plan.md:182` |
| **low** | `docs/07-planning/phases.md:109-125` (P1 scope) does not list an in-app notification inbox | `docs/03-features/notifications.md:3` "**Phase:** P4 (**in-app inbox in P1**)" and `docs/02-design/screen-inventory.md:26` "\| Inbox \| `/agent/inbox` \| route \| **P1** \| ⬜ \|" | The spec and the inventory agree, so **P1** wins; phases.md is the omission | Add to `phases.md` P1: "- In-app notification inbox (`/agent/inbox`) — channels, preferences, digests and quiet hours follow in P4 (notifications.md (`../03-features/notifications.md`))" |
| **low** | `docs/07-planning/review-2026-09-05.md:214` "the screen inventory **recounted (133)** with a `kind` column" | `docs/02-design/screen-inventory.md:208` "\| **Total** \| **136** \|"; `status.md:35-36` "0 of **136**"; `decision-log.md:65` "screen inventory **136** (was 133 after the first audit…)"; `CHANGELOG.md:22` "screen inventory (**133 at that pass; 136 after the closure**…)" | **136**. CHANGELOG.md shows the correct way to write a historical count; review-2026-09-05.md gives 133 with no "at that pass" qualifier, so it reads as current | In `review-2026-09-05.md:214` write "the screen inventory recounted (**133 at that pass; 136 after the same-day closure pass**) with a `kind` column" |
| **low** | `docs/07-planning/phases.md:227` "The **fourteen** reports, in five groups"; `accelerated-delivery-plan.md:96` "14 reports"; `:197` "a working subset of **the fourteen**"; `docs/00-overview/vision.md:44` "14 reports"; `docs/03-features/reports-and-dashboards.md:48` "**Tier 1 — Fixed reports.** The fourteen reports below" | `docs/07-planning/reviews/2026-09-05/features-governance-design.md:304` (a **high**, still-open finding): "**Twenty reports are listed, described as fourteen** … the tables contain 4 + 3 + 5 + 4 + 4 = **20**", repeated at `:579` and carried into `review-2026-09-05.md:69-70` "**report definitions** (twenty reports…)" | The **spec's own tables** win on the count (20); "fourteen" is wrong in five documents. This is an open stage-2 finding, so the planning-layer fix is to stop propagating a number that is already known to be wrong | Resolve the count in `reports-and-dashboards.md` first (it owns it), then update `phases.md:227`, `accelerated-delivery-plan.md:96,197` and `vision.md:44` in the same change. Until then, the planning docs should say "the fixed report set" rather than a number |
| **medium** | `docs/07-planning/phases.md:77-79` "**P0 step 0 — spec closure.** **Before step 1**, the [planning review]'s prerequisites are **finished, in this order**, so nothing below is built on a guess:" — followed by four numbered items; and `docs/07-planning/status.md:128` "a **step 0** (spec closure — **done**, see phases.md (`phases.md#p0--foundation`)) now precedes step 1" | The fourth item, `docs/07-planning/phases.md:91-94`, contradicts that framing: "Each feature spec's remaining findings in [reviews/2026-09-05/] are closed **at SDLC stage 2 of that feature, before its build starts — not all at once now**." `status.md:132-135` agrees with *that*: item 4 "is a **standing gate** at that feature's SDLC stage 2, **not a completed step**" | Item 4's own wording wins (it matches `review-2026-09-05.md:63-68` "What is deliberately left to stage 2, per feature" and `AGENTS.md` do-not 15). The header sentence and status.md:128's flat "done" are the errors | In `phases.md:77-79` change to "**Before step 1**, items 1–3 are finished, in this order …; item 4 is a **standing gate at each feature's SDLC stage 2**, not a step 0 task"; in `status.md:128` change "spec closure — done" to "spec closure — **items 1–3 done; item 4 is a standing per-feature gate**" |
| **low** | `docs/07-planning/status.md:38` "**Docs:** **~125 files**, link check clean" | `docs/07-planning/review-2026-09-05.md:10` "every relative link and heading anchor in **104 markdown files** checked by script (zero broken links…)". Actual on disk: **123** `.md` under `docs/`, **126** in the repo | status.md's "~125" is correct (the `~` covers 123/126); review-2026-09-05.md's 104 is a pre-closure snapshot presented as the current link-check scope | In `review-2026-09-05.md:10` write "104 markdown files **as the corpus stood at that pass** (126 after the same-day closure additions)" — or re-run the check and state the current number |
| **low** | `docs/07-planning/accelerated-delivery-plan.md:130` workstream **E — Realtime, notifications, webhooks**: "WebSocket fan-out, **notification channels**, outbound webhooks with signing" — "channels", plural and unqualified, in weeks 2–4 | `docs/07-planning/roadmap.md:85` "**Notification / chat integrations** \| Future only, in this order: **Email is core**; then Microsoft Teams → Slack → Telegram → Viber → others … **After P4**"; `docs/03-features/notifications.md:24-27` — only **Email (SMTP)** is core, everything else is future | roadmap.md and notifications.md win: exactly **one** channel plugin is in scope. Workstream E's plural invites an agent to build a chat channel that is explicitly deferred | In `accelerated-delivery-plan.md:130` change to "notification channels — **`notify.email` (SMTP) only; chat channels are deferred** (roadmap.md (`roadmap.md#explicitly-deferred-beyond-the-current-three-to-four-month-scope-decided-2026-09-05`))" |

## Verified consistent — no finding

Checked and in agreement across every document read, recorded so the next reviewer does not redo it:

- **Feature → phase, specs vs index.** All **31** specs' `**Phase:**` header fields match `docs/03-features/README.md` exactly, including the four parenthetical carve-outs (`god-mode.md:3` "P4 (authentication and organisations land in P3; the audit log in P2)", `identity-provisioning.md:3`, `notifications.md:3`, `projects-and-engagements.md:3`, `settings-hierarchy.md:3`, `teams.md:3`). The only phase disagreement found anywhere is assignment (row above).
- **Counts.** Screens **136** and the per-phase split (P0 6 · P1 33 · P2 18 · P3 21 · P4 28 · P5 28 · P6 2 = 136) agree in `screen-inventory.md:201-208`, `status.md:35-36` and `decision-log.md:65`. Features **31** agree in `README.md` (31 rows), 31 spec files on disk, `status.md:37`, `decision-log.md:68`, `accelerated-delivery-plan.md:41`. ADRs **0001–0013** = 13 files agree everywhere. God Mode **nineteen** agrees in `god-mode.md:52,300`, `decision-log.md:68`, `status.md:220` and the 19 God Mode rows in `screen-inventory.md:151-169`.
- **Notification channels (task item 4).** Email/SMTP is **core** in all five places it appears (`notifications.md:24`, `roadmap.md:85`, `decision-log.md:59`, `plugin-architecture.md:161`, `inherited-features.md:16`). The chat order is **Teams → Slack → Telegram → Viber** in all five, never varied. **Discord is never a prioritised channel** — it appears only as "Future, **unprioritised**" (`notifications.md:27`, `plugin-architecture.md:162`) or in the removed-at-fork list. The **generic signed webhook is core P4** in `phases.md:203` and workstream E, and appears in no deferral list.
- **Deferred list (task item 5).** No file schedules antivirus, Postgres RLS, chat integrations, dev-tool links, further IdPs (Keycloak is "future" in `phases.md:173`), SCIM `/Bulk`, or public boards. Public-board deletion is consistent in `phases.md:39`, `roadmap.md:87`, `accelerated-delivery-plan.md:113,190`, `risks.md:377`, `decision-log.md:29,625-635`. Marketplace is the sole exception, and it is the finding above.
- **Risks (task item 8).** R1–R20 are each defined exactly once in `risks.md`, numbered consecutively, and every risk cited from `status.md`'s watch list (R15, R2, R1, R17) and from the other planning documents (R3, R4, R7, R12, R19, R20) matches the meaning at its definition. No risk is numbered twice. The only issue is R15–R17 having a second, duplicate home (row above).
- **SCIM + Entra OIDC as core P3.** `README.md:38`, `identity-provisioning.md:3`, `phases.md:166-186`, `release-plan.md:71`, `accelerated-delivery-plan.md:127` and `decision-log.md:42-51` all place it in P3 with the same gate (17 acceptance tests against a real Entra tenant).

## Top 5

1. **`phases.md:270-273` still schedules the AWS Marketplace listing as ordinary P7 work**, while `roadmap.md:84`, `accelerated-delivery-plan.md:88,173` and `decision-log.md:58` defer it beyond the whole 3–4-month scope. phases.md is the document an implementer reads for "what is in P7" — fix it there first.
2. **`release-plan.md:75,89,108` puts "marketplace packaging" inside `2.3.0` and asserts `2.3` lands inside the 3-month window** — the exact window `accelerated-delivery-plan.md:88` says marketplace is deferred *beyond*. One release table contradicting the deferral in two cells.
3. **13 of the 31 feature specs are in no workstream and in no deferral row** (`accelerated-delivery-plan.md:124-131` vs `:182-183`) — custom fields, teams, MCP, API keys, automations, impersonation, settings hierarchy, audit trail, attachments, relations, saved views, projects, pending actions. The register's own promise is that nothing is silently thinner; these are.
4. **The inherited-in-week-1 correction was annotated, not applied.** `accelerated-delivery-plan.md:185-193` says calendar/timeline, basic time entries and the `workflow-rule` engine arrive with the fork and ship flagged off, but `:198-199` still say "deferred" / "Not present", there is no automations row at all, and `phases.md:202,224-225` never mention the inherited code — even though `decision-log.md:244-246` declares "that register **is corrected**".
5. **`decision-log.md:388-401` ("AWS Marketplace is the first external sales channel … lands in P7") carries no supersession note**, unlike `:196` and `:234` which do. Same-day supersession is the log's normal pattern; this entry and `:514` ("No dates on the roadmap until P1 closes") are the two that read as live decisions and are not.
