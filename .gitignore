# ── Dependencies ────────────────────────────────────────────────────────────
node_modules/
.pnpm-store/
.yarn/

# ── Build output ────────────────────────────────────────────────────────────
dist/
build/
out/
.next/                 # apps/site (Next.js + Fumadocs)
storybook-static/
*.tsbuildinfo
.turbo/
.vite/
.cache/
.rollup.cache/

# ── Test artefacts ──────────────────────────────────────────────────────────
coverage/
.nyc_output/
playwright-report/
test-results/
blob-report/
playwright/.cache/
tests/visual/**/*-actual.png
tests/visual/**/*-diff.png
k6-results/

# ── Environment and secrets — never commit; deploy/.env.example is the template
.env
.env.*
!.env.example
!deploy/.env.example
# repository-bootstrap.md §1 copies apps/api/.env.test.example; the `.env.*` rule
# above would otherwise silently drop it. It holds test placeholders, no secrets.
!.env.test.example
*.pem
*.key
*.crt
!tests/fixtures/**/*.pem
deploy/local/certs/
deploy/local/data/
*.sqlite
*.sqlite3

# ── Generated at build/release time, not source ─────────────────────────────
sbom*.json
sbom*.xml
apps/site/public/openapi.json
.sentryclirc

# ── Logs ────────────────────────────────────────────────────────────────────
*.log
logs/
pnpm-debug.log*
npm-debug.log*
yarn-error.log*
lerna-debug.log*

# ── Editors and OS ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.history/

# ── Local tooling state ─────────────────────────────────────────────────────
.eslintcache
.biome-cache/
tmp/
temp/
*.local

# --- inherited from kaneo at 42bb8011 (repository-bootstrap.md §1, `.gitignore` = Copy) ---
node_modules
.pnp
.pnp.js
.env.local
.env.development.local
.env.test.local
.env.production.local
coverage
.turbo
.vercel
.next/
build
dist
.source
yarn-debug.log*
*.db
.worktrees/
compose.override.yml
.idea
apps/web/.tanstack/
.pi/

# Generated by scripts/deploy.sh — key material and local certificates.
# None of it is version-controlled; deploy/.env.example is the template.
deploy/local/certs/
deploy/traefik/dynamic/tls-local.yml
deploy/seaweedfs/s3.json
